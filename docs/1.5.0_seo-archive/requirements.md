# v1.5.0 SEO 优化需求文档

> 版本：1.5.0 | 日期：2026-02-07 | 状态：需求评审

---

## 背景

当前项目 SEO 基础评分 **2/10**，核心问题：

1. 搜索引擎爬虫抓到的是**空壳页面**（首页/详情页数据全部客户端 fetch，爬虫看不到内容）
2. 所有页面共用同一个 title/description，详情页无动态 Metadata
3. 无 sitemap / robots.txt / Open Graph / 结构化数据
4. 社交分享只显示纯文字链接，无预览图

**目标**：让搜索引擎能正确发现、抓取、索引所有 Skill 详情页，并在社交分享时展示丰富的预览信息。

---

## 现状审计

| 项目 | 现状 | 文件位置 |
|------|------|----------|
| 全局 Metadata | ✅ 仅 title + description | `app/layout.tsx` |
| 首页 Metadata | ❌ 无 generateMetadata | `app/page.tsx` |
| 详情页 Metadata | ❌ 无 generateMetadata | `app/skill/[id]/page.tsx` |
| sitemap.xml | ❌ 缺失 | — |
| robots.txt | ❌ 缺失 | — |
| Open Graph | ❌ 缺失 | — |
| Twitter Card | ❌ 缺失 | — |
| 结构化数据 (JSON-LD) | ❌ 缺失 | — |
| 首页数据渲染 | ⚠️ 纯 CSR（爬虫看不到） | `components/home/HomePage.tsx` |
| 详情页数据渲染 | ⚠️ 纯 CSR（爬虫看不到） | `components/detail/DetailPage.tsx` |
| 404 页面 | ❌ 缺失 | — |
| canonical URL | ❌ 缺失 | — |
| PWA Manifest | ✅ 已配置 | `public/site.webmanifest` |
| lang 属性 | ✅ zh-CN | `app/layout.tsx` |

---

## 需求清单

### P0：基础设施（必做）

#### 1. 详情页动态 Metadata

**目标**：每个 Skill 详情页拥有独立的 title、description、OG 标签

**实现方案**：
- 在 `app/skill/[id]/page.tsx` 中添加 `generateMetadata` 函数
- 服务端直接查询 Supabase 获取 Skill 的 name、description、tag 等信息
- 生成规则：
  - `title`：`{skill.name} - Skill Hub 中国`
- `description`：`{skill.description}`（中文建议控制在约 80-120 字符，避免过长被截断）
  - `openGraph.title`：同 title
  - `openGraph.description`：同 description
  - `openGraph.type`：`"article"`
  - `openGraph.url`：`https://{domain}/skill/{id}`
  - `openGraph.siteName`：`"Skill Hub 中国"`
  - `twitter.card`：`"summary_large_image"`

**注意事项**：
- `generateMetadata` 在 Server Component 中运行，可以直接用 `getSupabaseServerClient` 查询
- 如果 Skill 不存在，应调用 `notFound()` 触发 404
- 考虑性能：这里只需要查 name/description/tag，不需要完整的 GitHub 同步

#### 2. 动态 sitemap.xml

**目标**：自动生成包含所有已上架 Skill 的 sitemap

**实现方案**：
- 新建 `app/sitemap.ts`，使用 Next.js 内置的 sitemap 生成 API
- 查询所有 `is_listed = true` 的 Skill
- 生成规则：
  - 首页 `/`：`changeFrequency: "daily"`, `priority: 1.0`
  - 详情页 `/skill/{id}`：`changeFrequency: "weekly"`, `priority: 0.8`
  - `lastModified`：使用 Skill 的 `updated_at` 或 `updated_at_sys`

**注意事项**：
- 如果 Skill 数量超过 50000，需要使用 `generateSitemaps` 做分页
- sitemap 需要包含完整的域名 URL（固定使用 https://www.skill-cn.com）

#### 3. robots.txt

**目标**：告知搜索引擎爬取规则

**实现方案**：
- 新建 `app/robots.ts`
- 规则：
  - `Allow: /`（允许爬取所有页面）
  - `Disallow: /api/`（禁止爬取 API 路由）
  - `Disallow: /auth/`（禁止爬取认证回调）
  - `Sitemap: https://{domain}/sitemap.xml`

#### 4. 全局 Open Graph 补全

**目标**：在 `app/layout.tsx` 中补全全局 OG 默认值

**实现方案**：
- 在全局 metadata 中添加（并补一个默认 `og:image`，在动态 OG 图上线前确保分享有预览图）：
  ```
  openGraph: {
    siteName: "Skill Hub 中国",
    locale: "zh_CN",
    type: "website",
    images: ["/og-cover.png"],
  }
  twitter: {
    card: "summary_large_image",
  }
  ```
- 这些作为默认值，会被详情页的 `generateMetadata` 覆盖

---

### P1：内容可索引（重要）

#### 5. 详情页 SSR 预渲染

**目标**：让搜索引擎爬虫能抓到详情页的核心内容

**现状问题**：
- `DetailPage.tsx` 是 `"use client"` 组件
- 数据通过 `useEffect` + `fetch('/api/skills/${id}?refresh=0')` 客户端加载
- 爬虫看到的只有骨架屏

**实现方案（选择一种）**：

**方案 A：Server Component 预取 + Client Hydration（推荐）**
- 在 `app/skill/[id]/page.tsx` 中服务端预取 Skill 数据（仅基础信息 + 缓存的 markdown）
- 将预取数据作为 props 传给 `DetailPage`
- `DetailPage` 使用预取数据做首屏渲染，同时客户端异步刷新 GitHub 数据
- 优点：爬虫能看到完整内容，用户体验不变
- 影响范围：`app/skill/[id]/page.tsx`、`DetailPage.tsx`（需要接受初始数据 props）

**方案 B：流式 SSR（Streaming）**
- 将 DetailPage 拆分为 Server Component（核心信息）+ Client Component（交互功能）
- 核心信息在服务端渲染，交互功能用 Suspense 包裹
- 优点：架构最优
- 缺点：改动量大，需要大幅重构 DetailPage

**建议采用方案 A**，改动量可控且效果显著。

#### 6. 首页 SSR 预渲染（首屏数据）

**目标**：让爬虫能抓到首页的 Skill 列表（至少第一页）

**实现方案**：
- 在 `app/page.tsx` 中服务端预取第一页 Skill 数据（默认排序，无筛选条件）
- 将预取数据作为 `initialSkills` props 传给 `HomePage`
- `HomePage` 使用 `initialSkills` 做首屏渲染，后续交互仍走客户端 fetch
- 精选实践轮播数据也可在服务端预取

**注意事项**：
- 只预取"默认状态"（无筛选、第一页），筛选/翻页仍走客户端
- 预取数据需要与客户端状态兼容，避免 Hydration mismatch

---

### P2：增强优化

#### 7. 结构化数据 (JSON-LD)

**目标**：在详情页注入 JSON-LD，帮助搜索引擎理解内容语义

**实现方案**：
- 在详情页的 Server Component 中注入 `<script type="application/ld+json">`
- Schema 类型使用 `SoftwareApplication` 或 `CreativeWork`（避免伪造评分：没有真实用户评分时不要输出 `aggregateRating`）：
  ```json
  {
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    "name": "{skill.name}",
    "description": "{skill.description}",
    "applicationCategory": "AI Tool",
    "operatingSystem": "Any",
    "author": {
      "@type": "Person",
      "name": "{skill.repo_owner_name}"
    }
  }
  ```

#### 8. OG 图片生成

**目标**：为每个 Skill 详情页生成动态 OG 预览图

**实现方案**：
- 新建 `app/skill/[id]/opengraph-image.tsx`，使用 Next.js 内置的 `ImageResponse`
- 图片内容：Skill 名称 + 标签 + 热度 + 品牌 Logo
- 尺寸：1200 x 630
- 在 `generateMetadata` 中引用该图片 URL

#### 9. 404 页面

**目标**：自定义 404 页面，保持品牌一致性

**实现方案**：
- 新建 `app/not-found.tsx`
- 展示品牌化的 404 提示 + "返回首页"按钮
- 在详情页 `generateMetadata` 中，如果 Skill 不存在则调用 `notFound()`

#### 10. canonical URL

**目标**：避免重复内容问题

**实现方案**：
- 在全局 metadata 中配置 `metadataBase`（固定使用 https://www.skill-cn.com）
- 在 `generateMetadata` 中设置 `alternates.canonical`
- 首页 canonical 固定为根路径（忽略 query 参数）
- 详情页 canonical 为 `/skill/{id}`

#### 11. 搜索参数页 noindex（P1 补充）

**目标**：避免 `/?q=xxx` 这类参数页被索引为空壳页面

**实现方案**：
- 对带查询参数的首页设置 `robots: { index: false }` 或 `noindex`
- 或者在 P1 同步完成“搜索结果 SSR”，保证可索引内容与标题一致

---

## 环境变量

本期无需新增环境变量，站点根 URL 固定为 `https://www.skill-cn.com`（用于 sitemap/canonical/OG）。

---

## 涉及文件清单

### 新建文件
| 文件 | 用途 |
|------|------|
| `app/sitemap.ts` | 动态 sitemap 生成 |
| `app/robots.ts` | robots.txt 配置 |
| `app/not-found.tsx` | 自定义 404 页面 |
| `app/skill/[id]/opengraph-image.tsx` | 动态 OG 图片（P2） |

### 修改文件
| 文件 | 改动内容 |
|------|----------|
| `app/layout.tsx` | 补全 openGraph/twitter 默认值、metadataBase |
| `app/page.tsx` | 添加 generateMetadata、SSR 预取首屏数据（P1） |
| `app/skill/[id]/page.tsx` | 添加 generateMetadata、SSR 预取 Skill 数据（P1） |
| `components/detail/DetailPage.tsx` | 接受预取数据 props（P1） |
| `components/home/HomePage.tsx` | 接受 initialSkills props（P1） |

---

## 验收标准

### P0 验收
- [ ] 详情页 `<title>` 显示为 `{Skill名称} - Skill Hub 中国`
- [ ] 详情页 `<meta name="description">` 显示 Skill 的 description
- [ ] 详情页有完整的 `og:title`、`og:description`、`og:url` 标签
- [ ] `/sitemap.xml` 可访问，包含所有已上架 Skill 的 URL
- [ ] `/robots.txt` 可访问，规则正确
- [ ] 不存在的 Skill ID 返回 404 状态码

### P1 验收
- [ ] `curl` 详情页 HTML 能看到 Skill 的名称、描述、标签等核心信息
- [ ] `curl` 首页 HTML 能看到第一页的 Skill 卡片内容
- [ ] Google Search Console 提交 sitemap 后，页面能被正常索引

### P2 验收
- [ ] 详情页 HTML 中包含 `<script type="application/ld+json">` 结构化数据
- [ ] 社交平台分享详情页链接时显示动态预览图
- [ ] 访问不存在的页面展示品牌化 404 页面

---

## 技术风险与注意事项

1. **Hydration 一致性**：SSR 预取数据传给 Client Component 时，需确保服务端和客户端渲染结果一致，否则会出现 Hydration mismatch 警告
2. **性能影响**：`generateMetadata` 中的 Supabase 查询会增加 TTFB，需要控制查询复杂度（只查必要字段）
3. **缓存策略**：sitemap 和 metadata 查询可以使用 `next.revalidate` 做 ISR 缓存，避免每次请求都查数据库
4. **DetailPage 改造范围**：方案 A 需要 DetailPage 支持从 props 接收初始数据，同时保留客户端刷新逻辑，改动需谨慎避免破坏现有功能
5. **环境变量**：站点根 URL 固定为 `https://www.skill-cn.com`，无需在部署环境单独配置

---

## 附录：SEO 审计完整报告

> 审计日期：2026-02-07 | 工具：代码静态分析 | 总评分：**2/10** 🔴

### 1. 爬取与索引 (Crawlability & Indexation)

| 检查项 | 状态 | 说明 |
|--------|------|------|
| robots.txt | ❌ 缺失 | 无 `app/robots.ts` 也无 `public/robots.txt`，爬虫无规则指引 |
| sitemap.xml | ❌ 缺失 | 无 `app/sitemap.ts` 也无 `public/sitemap.xml`，爬虫无法发现详情页 |
| 站点架构 | ⚠️ 两层 | 首页 → 详情页，架构简洁但缺少分类聚合页 |
| 爬取预算 | ✅ 无问题 | 页面数量少，无需担心爬取预算 |
| 孤立页面 | ⚠️ 存在 | 实践文章无独立页面（纯外链跳转），无法被索引 |

#### 关键问题：首页和详情页对爬虫是"空壳"

**首页**（`app/page.tsx`）：
- 页面本身是 Server Component，但只渲染了 `<HomePage>` 这个 Client Component
- `HomePage`（`"use client"`）的所有 Skill 数据通过 `useEffect` + `fetch('/api/skills?...')` 客户端加载
- **爬虫看到的**：Header + Footer + 空的内容区域

**详情页**（`app/skill/[id]/page.tsx`）：
- 同理，`DetailPage`（`"use client"`）的数据通过 `fetch('/api/skills/${id}?refresh=0')` 客户端加载
- **爬虫看到的**：Header + Footer + 骨架屏占位符

### 2. 页面标题与描述 (Title Tags & Meta Descriptions)

| 页面 | title | description | 状态 |
|------|-------|-------------|------|
| 全局默认 | `Skill Hub 中国` | `助力国内 Skill 使用者快速找到能用、好用、可复用的实践方案` | ⚠️ 有但固定 |
| 首页 `/` | 继承全局 | 继承全局 | ❌ 无动态 metadata |
| 详情页 `/skill/[id]` | 继承全局 | 继承全局 | ❌ 无 generateMetadata |
| 搜索 `/?q=xxx` | 继承全局 | 继承全局 | ❌ 不反映搜索词 |

**问题**：所有页面标题/描述完全相同，搜索引擎无法区分页面，严重影响索引质量。

### 3. Open Graph & 社交分享

| 检查项 | 状态 |
|--------|------|
| og:title | ❌ 缺失 |
| og:description | ❌ 缺失 |
| og:image | ❌ 缺失 |
| og:url | ❌ 缺失 |
| og:site_name | ❌ 缺失 |
| og:type | ❌ 缺失 |
| og:locale | ❌ 缺失 |
| twitter:card | ❌ 缺失 |
| twitter:title | ❌ 缺失 |
| twitter:description | ❌ 缺失 |
| twitter:image | ❌ 缺失 |

**影响**：在微信/微博/Twitter 等平台分享链接时，只显示纯文字 URL，无标题、无描述、无预览图。

### 4. 结构化数据 (Structured Data)

- ❌ 无任何 JSON-LD `<script type="application/ld+json">`
- ❌ 无 schema.org 标记
- ❌ Skill 详情页缺少 `SoftwareApplication` / `CreativeWork` schema
- ❌ 无 `Organization` / `WebSite` schema
- **影响**：无法在搜索结果中展示富媒体摘要（Rich Snippets）

### 5. URL 结构

| 检查项 | 状态 | 说明 |
|--------|------|------|
| URL 格式 | ⚠️ | `/skill/123` 使用数字 ID，不含语义关键词（如 `/skill/react-dev-assistant`） |
| 一致性 | ✅ | 结构统一 |
| 大小写 | ✅ | 全小写 |
| canonical | ❌ 缺失 | 无 canonical 标签，`?tag=编程` 等参数页面可能被视为重复内容 |
| metadataBase | ❌ 缺失 | 未配置，无法生成完整的绝对 URL |

### 6. 标题层级 (Heading Structure)

| 页面 | H1 | 状态 |
|------|------|------|
| 首页 | 有 H1（CSR） | ❌ H1 仅在客户端渲染，爬虫看不到 |
| 详情页 | 客户端渲染 | ❌ H1 由 JS 动态插入，爬虫看不到 |

### 7. 图片优化

| 检查项 | 状态 | 说明 |
|--------|------|------|
| alt 属性 | ⚠️ 部分 | SkillCard 头像有 alt（`skill.repo_owner_name`），但 alt 值可能为空字符串 |
| 图片格式 | ✅ | 外部 GitHub 头像，格式由源控制 |
| 懒加载 | ⚠️ | 未使用 Next.js `<Image>` 组件（用原生 `<img>`），无自动懒加载/优化 |
| 响应式 | ❌ | 无 srcset/sizes 属性 |

### 8. 其他技术 SEO

| 检查项 | 状态 | 说明 |
|--------|------|------|
| HTTPS | ✅ | 由部署平台保证 |
| lang 属性 | ✅ | `<html lang="zh-CN">` |
| viewport | ✅ | Next.js 默认注入 |
| 404 页面 | ❌ | 无 `app/not-found.tsx` |
| error 页面 | ❌ | 无 `app/error.tsx` |
| hreflang | — | 单语言站点，暂不需要 |
| PWA | ✅ | `site.webmanifest` 配置完整 |
| 主题色 | ✅ | `themeColor: "#FF6B00"` |
| favicon | ✅ | 完整配置（ico/svg/png/apple-touch-icon） |

### 9. SkillCard 的 `target="_blank"` 体验取舍

```tsx
// components/home/SkillCard.tsx 第 12-14 行
target="_blank"
rel="noreferrer"
```

- 首页所有 Skill 卡片链接都在**新标签页**打开
- 这更偏向体验选择，不一定是 SEO 硬伤
- `rel="noreferrer"` 会阻止 Referer 传递（影响站内/站外归因）

### 10. 搜索功能对 SEO 的影响

```tsx
// components/AppHeader.tsx 第 77 行
<form className="search" role="search" action="/" method="get">
```

- 搜索使用 GET 表单提交，生成 `/?q=xxx` URL
- 这些带参数的 URL 可能被搜索引擎索引为独立页面
- 但因为首页数据是 CSR，搜索引擎抓不到搜索结果内容
- 缺少 `noindex` 标记来防止空搜索结果页被索引

### 11. 优先修复清单

| 优先级 | 问题 | 影响 | 工作量 |
|--------|------|------|--------|
| 🔴 P0 | 详情页无 generateMetadata | 所有详情页不可被有效索引 | 小 |
| 🔴 P0 | 无 sitemap.xml | 爬虫无法发现详情页 | 小 |
| 🔴 P0 | 无 robots.txt | 爬虫无规则指引 | 小 |
| 🔴 P0 | 无 Open Graph 标签 | 社交分享效果为零 | 小 |
| 🟠 P1 | 详情页数据 CSR | 爬虫抓到空壳 | 中 |
| 🟠 P1 | 首页数据 CSR | 爬虫抓到空壳 | 中 |
| 🟠 P1 | 无 canonical URL | 参数页面可能被视为重复内容 | 小 |
| 🟠 P1 | 搜索参数页无 noindex/SSR | 可能索引空结果页 | 小 |
| 🟡 P2 | 无结构化数据 | 无 Rich Snippets | 中 |
| 🟡 P2 | 无 OG 图片 | 分享无预览图 | 中 |
| 🟡 P2 | 无 404/error 页面 | 错误体验差 | 小 |
| 🟢 P3 | URL 使用数字 ID 而非 slug | 语义化不足 | 大（需改数据库） |
| 🟢 P3 | SkillCard target="_blank" | 用户参与度信号弱 | 小 |
| 🟢 P3 | 图片未用 Next/Image | 无自动优化/懒加载 | 中 |
